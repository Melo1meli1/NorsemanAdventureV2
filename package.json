{
  "dependencies": {
    "@supabase/supabase-js": "^2.93.1"
  },
  "devDependencies": {
    "eslint": "^9",
    "husky": "^9",
    "lint-staged": "^16",
    "prettier": "^3"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,css,md}": [
      "prettier --write"
    ],
    "!package-lock.json": []
  },
  "scripts": {
    "prepare": "husky"
  }
}
